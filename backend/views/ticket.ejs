<!DOCTYPE html>
<html>

<head>
  <title>Ticket Details</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
  <a href="/" class="text-white bg-blue-500 hover:bg-blue-700 font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
    Home
  </a>
  <div class="container mx-auto px-4 py-8">
    <div class="bg-white rounded-lg shadow-md p-6">
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold">Ticket Details</h1>
        <p class="text-gray-600">Logged in as: <%= user.name %></p>
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <div class="space-y-2">
            <p><strong>VATIN:</strong> <%= ticket.vatin %></p>
            <p><strong>Name:</strong> <%= ticket.first_name %> <%= ticket.last_name %></p>
            <p><strong>Created:</strong> <%= new Date(ticket.created_at).toLocaleString() %></p>
            <p><strong>Ticket ID:</strong> <%= ticket.id %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>